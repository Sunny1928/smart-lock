# speaker verification


## Conda

### Create environment
    conda create -n test python=3.10.12
    conda activate test
    pip install -r /path/to/requirements.txt
    
### Run code
    python run.py